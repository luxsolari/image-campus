#pragma once
#include <thread>
#include <iostream>
#include <vector>
#include <string>
#include <Windows.h>
using namespace std;  // NOLINT(clang-diagnostic-header-hygiene)

constexpr int INTENTOS_MAX = 100;
constexpr int MAX_ARRAY_SIZE = 35;
const string COMANDO_RENDIRSE = "/MERINDO";
